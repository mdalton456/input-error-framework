'use strict';

describe('Service: PoInputRefData', function () {

  // load the service's module
  beforeEach(module('exampleFrontEndApplicationApp'));

  // instantiate service
  var PoInputRefData;
  beforeEach(inject(function (_PoInputRefData_) {
    PoInputRefData = _PoInputRefData_;
  }));

  it('should do something', function () {
  });

});
